# Site permission policy

ChatGPT Computer Use site permissions are maintained separately from Chrome extension host permissions. Central Agent may prepare and verify exact-domain requirements, but the final ChatGPT site-permission UI remains a user-controlled security gate. The normal user request after automation completes is therefore only to confirm the listed exact domains in ChatGPT > Computer Use > Google Chrome > Site permissions.
