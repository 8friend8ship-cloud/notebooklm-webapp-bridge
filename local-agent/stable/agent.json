{
  "schemaVersion": 2,
  "channel": "stable",
  "enabled": true,
  "version": "1.1.40",
  "releasedAt": "2026-08-28T18:47:00+09:00",
  "gitBlobSha1": "d491891efdf7039209e7334fa10dcd4123272a59",
  "file": "HomeDesignLocalAgent.ps1",
  "pollSeconds": 300,
  "maxCycleSeconds": 900,
  "notes": "Runtime-safe bridge automation repair for old 180s bootstrap installations. The agent itself stays short: hash-patches local AgentBootstrap.ps1 to configurable 900s cycle support, hash-fetches the NotebookLM exact-target x2 regression wrapper, dispatches it hidden in the background exactly once with a no-blind-retry marker, writes a central Drive dispatch receipt, and exits before the legacy parent timeout. Subsequent cycles only read the result/marker; they never duplicate the probe. Exact-target regression requires the known notebook URL, CDP target verification, input sentinel fill/readback/restore x2, and normal Chrome preservation. No Generate/credits/OAuth/scope/settings/public/destructive action."
}
