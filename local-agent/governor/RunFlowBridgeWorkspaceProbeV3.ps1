param([string]$TaskId='FLOW_WORKSPACE_PROBE_V3',[int]$DebugPort=9224,[string]$CentralRootOverride='')
$ErrorActionPreference='Stop';$ProgressPreference='SilentlyContinue'
$Base=Join-Path $env:LOCALAPPDATA 'HomeDesignAutomationV7';$UserData=Join-Path $Base 'ChromeUserData';$CftRoot=Join-Path $Base 'ChromeForTesting';$NotebookExt=Join-Path $Base 'Extension\NotebookLM-WebApp-Bridge';$NotebookFront='https://notebooklm-webapp-bridge.vercel.app/';$FlowUrl='https://labs.google/fx/tools/flow';$ExpectedId='lgedgmpcikglaajhfclcihicgafimlna'
function FindCentral{if($CentralRootOverride -and (Test-Path -LiteralPath $CentralRootOverride -PathType Container)){return $CentralRootOverride};$target=[Text.Encoding]::UTF8.GetString([Convert]::FromBase64String('MDBf7KSR7JWZ7JeQ7J207KCE7Yq4'));$ko=[Text.Encoding]::UTF8.GetString([Convert]::FromBase64String('64K0IOuTnOudvOydtOu4jA=='));foreach($d in @(Get-PSDrive -PSProvider FileSystem -ErrorAction SilentlyContinue)){$r=[string]$d.Root;if(!$r){continue};foreach($c in @((Join-Path $r $target),(Join-Path (Join-Path $r 'My Drive') $target),(Join-Path (Join-Path $r $ko) $target),(Join-Path (Join-Path $r 'Google Drive') $target))){if(Test-Path -LiteralPath $c -PathType Container){return $c}}};return ''}
function FindFlowExt{$c=@((Join-Path $env:USERPROFILE 'Downloads\flow-agent-bridge-v0.1.0\flow-agent-bridge-v0.1.0'),(Join-Path $env:USERPROFILE 'Downloads\flow-agent-bridge-v0.1.0'),(Join-Path $Base 'Extension\Flow-Agent-Bridge'),(Join-Path $Base 'Extension\Google-AI-Local-Bridge-Flow'));foreach($p in $c){if(Test-Path -LiteralPath (Join-Path $p 'manifest.json') -PathType Leaf){return $p}};return ''}
function FindChrome{Get-ChildItem -LiteralPath $CftRoot -Recurse -Filter chrome.exe -File -ErrorAction SilentlyContinue|Sort-Object FullName -Descending|Select-Object -First 1}
function Dedicated{try{@(Get-CimInstance Win32_Process -Filter "Name='chrome.exe'" -ErrorAction SilentlyContinue|Where-Object{$_.CommandLine -and ([string]$_.CommandLine).Contains($UserData)})}catch{@()}}
function NormalRoots{try{@(Get-CimInstance Win32_Process -Filter "Name='chrome.exe'" -ErrorAction SilentlyContinue|Where-Object{(-not $_.CommandLine -or -not([string]$_.CommandLine).Contains($UserData)) -and (-not $_.CommandLine -or ([string]$_.CommandLine)-notmatch '(?i)(^|\s)--type=')}|ForEach-Object{[int]$_.ProcessId})}catch{@()}}
function StopDedicated{foreach($p in @(Dedicated)){try{& taskkill.exe /PID ([int]$p.ProcessId) /T /F 2>$null|Out-Null}catch{}};Start-Sleep -Seconds 2}
function ReadText([string]$p,[int]$max=16000){if(!(Test-Path -LiteralPath $p -PathType Leaf)){return ''};$t=Get-Content -LiteralPath $p -Raw -Encoding UTF8;if($t.Length -gt $max){return $t.Substring(0,$max)};return $t}
function WriteAtomic([string]$p,$o){$d=Split-Path -Parent $p;New-Item -ItemType Directory -Force -Path $d|Out-Null;$tmp=$p+'.tmp';$o|ConvertTo-Json -Depth 60|Set-Content -LiteralPath $tmp -Encoding UTF8;Move-Item $tmp $p -Force}
function RestoreNotebook{try{StopDedicated;$c=FindChrome;if(!$c){return $false};$a=@("--user-data-dir=$UserData",'--profile-directory=Default','--new-window','--no-first-run','--no-default-browser-check','--disable-session-crashed-bubble');if(Test-Path -LiteralPath (Join-Path $NotebookExt 'manifest.json') -PathType Leaf){$a+=("--load-extension=$NotebookExt")};$a+=$NotebookFront;Start-Process -FilePath $c.FullName -ArgumentList $a -WorkingDirectory $c.Directory.FullName|Out-Null;Start-Sleep -Seconds 2;return (@(Dedicated).Count -gt 0)}catch{return $false}}
$central=FindCentral;if(!$central){throw 'CENTRAL_ROOT_NOT_FOUND'};$ext=FindFlowExt;if(!$ext){throw 'FLOW_EXTENSION_NOT_FOUND'};$manifest=Get-Content -LiteralPath (Join-Path $ext 'manifest.json') -Raw -Encoding UTF8|ConvertFrom-Json;$content=ReadText (Join-Path $ext 'content.js');$popupHtml=ReadText (Join-Path $ext 'popup.html');$popupJs=ReadText (Join-Path $ext 'popup.js');$arch=$(if($manifest.background.service_worker){'background_service_worker'}elseif($manifest.action.default_popup -and @($manifest.content_scripts).Count -gt 0){'content_script_plus_popup'}elseif(@($manifest.content_scripts).Count -gt 0){'content_script_only'}else{'unknown'});$contentConfigured=(@($manifest.content_scripts|Where-Object{@($_.matches) -contains 'https://labs.google/*' -and @($_.js) -contains 'content.js'}).Count -gt 0)
$normalBefore=@(NormalRoots);StopDedicated;$chrome=FindChrome;if(!$chrome){throw 'CFT_NOT_FOUND'};$args=@("--user-data-dir=$UserData",'--profile-directory=Default','--new-window','--no-first-run','--no-default-browser-check','--disable-session-crashed-bubble',("--load-extension=$ext"),("--remote-debugging-port=$DebugPort"),'--remote-debugging-address=127.0.0.1',$FlowUrl);Start-Process -FilePath $chrome.FullName -ArgumentList $args -WorkingDirectory $chrome.Directory.FullName|Out-Null
$ver=$null;$pages=@();$deadline=(Get-Date).AddSeconds(30);do{try{$ver=Invoke-RestMethod -Uri ("http://127.0.0.1:$DebugPort/json/version") -TimeoutSec 2;$pages=@(Invoke-RestMethod -Uri ("http://127.0.0.1:$DebugPort/json/list") -TimeoutSec 2);if($ver.webSocketDebuggerUrl -and @($pages|Where-Object{$_.type -eq 'page' -and ([string]$_.url).Contains('/fx/tools/flow')}).Count -gt 0){break}}catch{};Start-Sleep -Milliseconds 500}while((Get-Date)-lt $deadline)
$page=$pages|Where-Object{$_.type -eq 'page' -and ([string]$_.url).Contains('/fx/tools/flow')}|Select-Object -First 1;$probe=[ordered]@{ok=$false;stage='NO_PAGE'}
if($ver.webSocketDebuggerUrl -and $page.webSocketDebuggerUrl){$mjs=Join-Path $env:TEMP ('flow-v3-'+[guid]::NewGuid().ToString('N')+'.mjs');$sentinel='CENTRAL_AGENT_FLOW_V3_'+(Get-Date -Format 'HHmmss');$code=@'
const pageWs=process.argv[2], sentinel=process.argv[3];
function conn(u){return new Promise((res,rej)=>{const ws=new WebSocket(u),p=new Map();let n=0;ws.onopen=()=>res({ws,send:(m,a={})=>new Promise((r,j)=>{const id=++n;p.set(id,{r,j});ws.send(JSON.stringify({id,method:m,params:a}))})});ws.onerror=rej;ws.onmessage=e=>{let x;try{x=JSON.parse(e.data)}catch{return};if(x.id&&p.has(x.id)){const q=p.get(x.id);p.delete(x.id);x.error?q.j(new Error(JSON.stringify(x.error))):q.r(x.result)}}})}
const c=await conn(pageWs);const lit=JSON.stringify(sentinel);const scan=`(()=>{const roots=[document],q=[document],seen=new Set(q);while(q.length){const r=q.shift();let a=[];try{a=[...r.querySelectorAll('*')]}catch{}for(const e of a){if(e.shadowRoot&&!seen.has(e.shadowRoot)){seen.add(e.shadowRoot);roots.push(e.shadowRoot);q.push(e.shadowRoot)}}}const vis=e=>{if(!e)return false;const s=getComputedStyle(e),r=e.getBoundingClientRect();return s.display!=='none'&&s.visibility!=='hidden'&&r.width>18&&r.height>18};const d=e=>[e.getAttribute?.('aria-label'),e.getAttribute?.('placeholder'),e.getAttribute?.('title'),e.textContent,e.value].filter(Boolean).join(' ').replace(/\\s+/g,' ').trim();let ins=[];for(const r of roots){try{ins.push(...r.querySelectorAll('textarea,input[type=text],input:not([type]),[contenteditable=true],[role=textbox]'))}catch{}}let best=null,score=-999;for(const e of [...new Set(ins)]){if(!vis(e)||e.disabled||e.readOnly)continue;const x=d(e).toLowerCase();let s=e.tagName==='TEXTAREA'?12:0;if(e.isContentEditable)s+=7;for(const w of ['prompt','describe','description','scene','video','image','create','what do you want','프롬프트','설명','장면','영상','이미지','생성','입력'])if(x.includes(w))s+=12;for(const w of ['search','검색','title','제목','name','이름'])if(x.includes(w))s-=20;if(s>score){score=s;best=e}}let fill={attempted:false,verified:false,restored:false};if(best&&score>=5){const old=('value'in best)?best.value:best.textContent;const v=${lit};fill.attempted=true;best.focus();if('value'in best)best.value=v;else best.textContent=v;best.dispatchEvent(new Event('input',{bubbles:true}));best.dispatchEvent(new Event('change',{bubbles:true}));fill.verified=(('value'in best)?best.value:best.textContent)===v;if('value'in best)best.value=old;else best.textContent=old;best.dispatchEvent(new Event('input',{bubbles:true}));best.dispatchEvent(new Event('change',{bubbles:true}));fill.restored=(('value'in best)?best.value:best.textContent)===old}let btn=[];for(const r of roots){try{btn.push(...r.querySelectorAll('button,[role=button],a'))}catch{}}const routes=[...new Set(btn)].filter(vis).map(e=>({text:d(e).slice(0,180),href:e.href||''})).filter(x=>/new project|create project|새 프로젝트|프로젝트 만들기/i.test(x.text)).slice(0,10);return {url:location.href,title:document.title,inputCandidateCount:ins.length,promptInputFound:!!(best&&score>=5),promptInputScore:score,promptInputDesc:best?d(best).slice(0,240):'',fill,routes}})()`;let r=await c.send('Runtime.evaluate',{expression:scan,returnByValue:true,userGesture:true});let first=r.result?.value||null,clicked=false,clickText='';if(first&&!first.promptInputFound&&first.routes?.length){const click=`(()=>{const all=[...document.querySelectorAll('button,[role=button],a')];const e=all.find(x=>/new project|create project|새 프로젝트|프로젝트 만들기/i.test((x.innerText||x.textContent||x.getAttribute('aria-label')||'')));if(!e)return {clicked:false,text:''};const t=(e.innerText||e.textContent||e.getAttribute('aria-label')||'').trim();e.click();return {clicked:true,text:t}})()`;const cr=await c.send('Runtime.evaluate',{expression:click,returnByValue:true,userGesture:true});clicked=!!cr.result?.value?.clicked;clickText=cr.result?.value?.text||'';if(clicked){await new Promise(r=>setTimeout(r,4500));r=await c.send('Runtime.evaluate',{expression:scan,returnByValue:true,userGesture:true})}}const out={ok:true,firstPage:first,navigatedToWorkspace:clicked,navigationText:clickText,page:r.result?.value||null};try{c.ws.close()}catch{};console.log(JSON.stringify(out));process.exit(0);
'@;Set-Content -LiteralPath $mjs -Value $code -Encoding UTF8;try{$node=Get-Command node.exe -ErrorAction SilentlyContinue;if(!$node){$node=Get-Command node -ErrorAction Stop};$raw=& $node.Source $mjs ([string]$page.webSocketDebuggerUrl) $sentinel 2>&1|Out-String;$last=($raw.Trim().Split("`n")|Select-Object -Last 1).Trim();$probe=$last|ConvertFrom-Json}catch{$probe=[ordered]@{ok=$false;stage='NODE_PROBE_ERROR';error=$_.Exception.Message;raw=$(if($raw){$raw.Trim()}else{''})}}finally{Remove-Item -LiteralPath $mjs -Force -ErrorAction SilentlyContinue}}
$restored=RestoreNotebook;$normalAfter=@(NormalRoots);$missing=@($normalBefore|Where-Object{$normalAfter -notcontains $_});$normalUntouched=($missing.Count -eq 0);$inputOk=($probe.ok -and $probe.page.promptInputFound -and $probe.page.fill.verified -and $probe.page.fill.restored);$ok=([bool]$ver.webSocketDebuggerUrl -and $page -and $contentConfigured -and $inputOk -and $restored -and $normalUntouched)
$dir=Join-Path (Join-Path $central 'Runtime_Readback') 'Flow_Bridge_Direct';$rp=Join-Path $dir ($TaskId+'_result.json');$ap=Join-Path $dir ($TaskId+'_ACK.json');$result=[ordered]@{ok=[bool]$ok;action='FLOW_BRIDGE_WORKSPACE_PROBE_V3';taskId=$TaskId;extensionName=[string]$manifest.name;extensionVersion=[string]$manifest.version;extensionArchitecture=$arch;contentScriptConfigured=[bool]$contentConfigured;sourceContract=[ordered]@{contentJs=$content;popupHtml=$popupHtml;popupJs=$popupJs};cdpReady=[bool]$ver.webSocketDebuggerUrl;flowPageFound=[bool]$page;probe=$probe;normalChromeUntouched=[bool]$normalUntouched;normalChromeMissingRootPids=$missing;notebookDedicatedRestored=[bool]$restored;generateClicked=$false;creditSpend=$false;oauthChanged=$false;chromeSettingsChanged=$false;at=(Get-Date).ToString('o')};WriteAtomic $rp $result;WriteAtomic $ap ([ordered]@{ack=[bool]$ok;taskId=$TaskId;resultPath=$rp;generateClicked=$false;creditSpend=$false;at=(Get-Date).ToString('o')});$result['resultPath']=$rp;$result['ackPath']=$ap;$result['ack']=[bool]$ok;$result|ConvertTo-Json -Depth 60 -Compress;if($ok){exit 0}else{exit 2}
